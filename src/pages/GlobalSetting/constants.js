export const DEFAULT_DEPOSIT_MIN_LIMIT = 20000; // Currently IDR only

export const DEFAULT_DEPOSIT_MAX_LIMIT = 1000000;

export const DEFAULT_WIRHDRAW_MIN_LIMIT = 20000; // Currently IDR only

export const DEFAULT_WIRHDRAW_MAX_LIMIT = 1000000;

export const MIN_MAX_CURRENCIES = ['IDR'];
